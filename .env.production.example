# FoosPulse Production Environment Configuration (Railway)
# Copy this file and configure for your Railway deployment

# =============================================================================
# WEB SERVICE (apps/web)
# =============================================================================
# Set this to your Railway API service URL (e.g., https://foospulse-api.up.railway.app)
NEXT_PUBLIC_API_URL=https://your-api-service.up.railway.app

# =============================================================================
# API SERVICE (apps/api)
# =============================================================================
# Railway PostgreSQL - Use the internal URL provided by Railway
# Format: postgresql+psycopg://user:password@host:port/database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Railway Redis - Use the internal URL provided by Railway
REDIS_URL=${{Redis.REDIS_URL}}

# Security - MUST be a secure random 32+ character string
# Generate with: openssl rand -base64 32
JWT_SECRET=GENERATE_A_SECURE_RANDOM_STRING_HERE

# CORS - Set to your production frontend URL
# Multiple origins can be comma-separated
API_CORS_ORIGINS=https://your-web-service.up.railway.app

# Disable debug mode in production
API_DEBUG=false

# Use JSON logs for Railway log aggregation
JSON_LOGS=true
LOG_LEVEL=INFO

# Artifacts storage (Railway provides ephemeral storage)
# For persistent storage, consider using Railway volumes or S3
ARTIFACTS_DIR=/data/artifacts

# =============================================================================
# WORKER SERVICE (apps/worker)
# =============================================================================
# Uses same DATABASE_URL and REDIS_URL as API
CELERY_BROKER_URL=${{Redis.REDIS_URL}}
CELERY_RESULT_BACKEND=${{Redis.REDIS_URL}}

# =============================================================================
# OPTIONAL: Integrations
# =============================================================================
# Slack webhook for notifications (leave empty to disable)
SLACK_WEBHOOK_URL=

# LLM mode (off|optional|required)
LLM_MODE=off
